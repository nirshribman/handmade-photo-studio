import fs from 'node:fs/promises';
import sharp from 'sharp';
const dir='artifacts/edges';await fs.mkdir(dir,{recursive:true});
const styles=[['soft','Soft deckle','Fine fuzz with restrained exposure of the paper'],['raw','Raw cotton','Dense short fibres and an irregular exposed support'],['pulled','Pulled fibres','Longer curling threads among short, tangled fibres'],['photo-sequence','Photo sequence','Real sample photo, full bleed, raw inner joins and gentler outside edges']];
for(const [id] of styles.slice(0,3)){await sharp(dir+'/'+id+'.png').extract({left:220,top:1000,width:1300,height:350}).png().toFile(dir+'/'+id+'-detail.png');}
const cards=styles.map(([id,name,note])=>'<article><h2>'+name+'</h2><p>'+note+'</p><a href="'+id+'.png"><img src="'+id+'.png" alt="Exported '+name+'" loading="lazy"></a>'+(id==='photo-sequence'?'':'<a href="'+id+'-detail.png"><img class="detail" src="'+id+'-detail.png" alt="Torn edge at export pixels" loading="lazy"></a>')+'<p><a href="'+id+'.png">Open full PNG</a> &middot; <a href="'+id+'-settings.json" download>Download settings</a></p></article>').join('');
const html='<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Torn edge studies - Handmade Photo Studio</title><style>body{margin:0;background:#eaece5;color:#2e382b;font:16px/1.5 system-ui,sans-serif}main{max-width:1250px;margin:auto;padding:35px 22px}h1{font:42px Georgia,serif;margin-bottom:10px}h2{font:27px Georgia,serif}a{color:#3c6240}header p{max-width:860px}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(min(100%,380px),1fr));gap:26px}article{background:#f7f8f3;padding:20px;border:1px solid #d4dacc;border-radius:10px}img{width:100%;display:block;margin:14px 0}.detail{background:#171715}footer{margin-top:36px}code{font-size:13px}</style><main><header><a href="/">Open the studio</a><h1>Torn edge studies</h1><p>Actual 2,400-pixel exports from renderer 0.5.0. The first three use the same plain dark sheet to isolate the edge treatment. The last uses a supplied photograph. Click a picture for full resolution.</p><p>In <b>Paper edges</b>, choose a starting point, then adjust <b>Outer edge fraying</b>, <b>Inner join fraying</b>, <b>Exposed paper</b>, <b>Fibre density</b>, <b>Fibre length</b> and <b>Long strands</b>. Fibre clumping is under Fine adjustments. Use Zoom area on the canvas to inspect a join.</p></header><div class="grid">'+cards+'</div><footer><p><a href="transparent.png">Transparent PNG</a> &middot; <a href="over-000000.png">Over black</a> &middot; <a href="over-ffffff.png">Over white</a> &middot; <a href="over-9f22c5.png">Over purple</a></p><p>To reuse settings, load <code>tests/fixtures/dark-paper.png</code> for the edge studies or <code>Sample/DSC_5377.jpg</code> for the sequence, then restore its settings JSON from the folder menu. Rendering is procedural; the references guide material character rather than an exact physical reconstruction.</p><p>Sample/reference photos and these local exports are excluded from Git.</p></footer></main></html>';
await fs.writeFile(dir+'/index.html',html);console.log('Edge gallery: http://127.0.0.1:5173/artifacts/edges/index.html');
